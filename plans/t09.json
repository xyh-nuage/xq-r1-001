{"schema":"remote_ci_plan.v1","steps":[{"argv":["python","-m","pip","install","-e","./l1[dev]"]},{"argv":["python","-c","import importlib.util,json,pathlib,tempfile,zipfile; from l1_processor.candidate_source import CandidateL0Source,build_evidence_index,get_direct_succeeded_artifacts; from l1_processor.research_slice_a_attachment_materialization import materialize_structured_attachment_observations,usable_succeeded_preview_artifacts; out=pathlib.Path('l1/evaluation/vnext/runtime/slice_a_l1_attachment_materialization'); out.mkdir(parents=True,exist_ok=True); d={'stage_reached':0,'slice_rows':0,'duplicate_keys':0,'fixture_db_count':0,'canonical_messages':0,'missing_slice_keys':0,'evidence_rows':0,'artifact_rows':0,'direct_artifact_links':0,'parser_input_artifact_links':0,'messages_materialized':0,'candidate_count':0,'signal_count':0,'exception_present':0,'exc_assertion':0,'exc_other':0}; stage=0; 
try:
 p=pathlib.Path('l1/tools/run_research_slice_a_l1_attachment_materialization.py'); spec=importlib.util.spec_from_file_location('slice_mat_runner',p); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); rows=m._load_jsonl(pathlib.Path('context/evaluation/company_shared_local_semantics_slice_a.jsonl')); keys=[str(x.get('stable_key') or '') for x in rows]; stage=1; d.update(stage_reached=stage,slice_rows=len(keys),duplicate_keys=len(keys)-len(set(keys))); z=zipfile.ZipFile('l1/tests/fixtures/obsolete_media_test_data.zip'); names=[n for n in z.namelist() if n.lower().endswith('.db')]; z.close(); d['fixture_db_count']=len(names); stage=2; d['stage_reached']=stage; td=tempfile.TemporaryDirectory(); db=pathlib.Path(td.name)/'canonical.db'; m._load_fixture_db(pathlib.Path('l1/tests/fixtures/obsolete_media_test_data.zip'),db); stage=3; d['stage_reached']=stage; source=CandidateL0Source(db); all_rows=source.load_messages(); by={str(r['stable_key']):m._message_dict(r) for r in all_rows}; d['canonical_messages']=len(by); missing=[k for k in keys if k not in by]; d['missing_slice_keys']=len(missing); stage=4; d['stage_reached']=stage; msgs=[by[k] for k in keys if k in by]; vids=[str(x.get('source_version_id') or '') for x in msgs]; ers=source.load_evidence_for(vids); ah=[str(r['file_hash']) for r in ers]; ars=source.load_artifacts_for(ah); d['evidence_rows']=len(ers); d['artifact_rows']=len(ars); eb,af,_=build_evidence_index(ers,ars); stage=5; d['stage_reached']=stage; 
 for msg in msgs:
  direct=get_direct_succeeded_artifacts(list(msg.get('evidence_refs') or []),eb,af); d['direct_artifact_links']+=len(direct); usable=usable_succeeded_preview_artifacts(direct); d['parser_input_artifact_links']+=len(usable); res=materialize_structured_attachment_observations(message=msg,direct_artifacts=usable); d['messages_materialized']+=1; d['candidate_count']+=len(res.candidates); d['signal_count']+=len(res.signals)
 stage=6; d['stage_reached']=stage; source.close(); td.cleanup()
except Exception as e:
 d['exception_present']=1; d['exc_assertion']=int(isinstance(e,AssertionError)); d['exc_other']=int(not isinstance(e,AssertionError)); d['stage_reached']=stage
(out/'stage_diag.json').write_text(json.dumps(d,indent=2,sort_keys=True),encoding='utf-8')"],"audit":{"type":"numeric_json","path":"l1/evaluation/vnext/runtime/slice_a_l1_attachment_materialization/stage_diag.json","fields":{"stage_reached":"int","slice_rows":"int","duplicate_keys":"int","fixture_db_count":"int","canonical_messages":"int","missing_slice_keys":"int","evidence_rows":"int","artifact_rows":"int","direct_artifact_links":"int","parser_input_artifact_links":"int","messages_materialized":"int","candidate_count":"int","signal_count":"int","exception_present":"int","exc_assertion":"int","exc_other":"int"}}},{"argv":["python","-c","import sys; sys.exit(1)"]}]}