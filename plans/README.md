# Plans

Registered target plans contain fixed argv/cwd definitions. Pytest commands receive aggregate-count auditing automatically. Evaluation commands that must expose public numeric results declare a strict `audit.type = numeric_json` block; see `../docs/audit-output.md`.
