name: t54-status
on:
  push:
    branches: [main]
    paths: ['.github/workflows/t54_status.yml']
permissions:
  contents: read
  actions: read
  issues: write
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Report t54 run state
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          body="$(gh run list --repo "$GITHUB_REPOSITORY" --workflow t54_immutable_first48_nonthink5000_backoff.yml --limit 1 --json databaseId,status,conclusion,createdAt,updatedAt,headSha)"
          gh issue create --repo "$GITHUB_REPOSITORY" --title "t54-status:${GITHUB_RUN_ID}" --body "$body"
