name: t29-disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 't29 disabled: user one-call authorization exhausted; no further provider calls allowed'
